{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/form.css') }}">
{% endblock %}

{% block title %}
    Acheter un bien
{% endblock %}

{% block body %}
<main>
    <a href="{{ path('advert_detail', {'id': advert.id}) }}" class="link-back">Retour à l'annonce</a>
    <h1>
        Acheter un bien
    </h1>
    {# TODO : dislay advert info #}
    <div class="form">
        {{ form(form) }}
    </div>
</main>
{% endblock %}
