{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/form.css') }}">
{% endblock %}

{% set current_path = app.request.get('_route') %}

{% block title %}
    {% if current_path is same as 'address_add' %}
        Ajouter 
    {% else %}
        Modifier 
    {% endif %}
        une addresse
{% endblock %}

{% block body %}
<main>
    <a href="{{ path('user_account') }}" class="link-back">Retour au compte</a>
    <h1>
        {% if current_path is same as 'address_add' %}
            Ajouter 
        {% else %}
            Modifier 
        {% endif %}
            une addresse
    </h1>
    <div class="form">
        {{ form(form) }}
    </div>
</main>
{% endblock %}
